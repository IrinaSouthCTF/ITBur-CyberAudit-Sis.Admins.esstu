# ITBur-CyberAudit-Sis.Admins.esstu
## Участники команды

- **Сандакова Ирина** - (капитан), К65-01 [Telegram](https://t.me/whatsupimira)
- Сараев Данила, К65-01
- Перфильев Денис, К65-02
- Большаков Олег, Б733-1 
- Савин Степан, Б733-2
- ВСГУТУ

---
## Описание проекта

`auditor.py` - консольное Python-приложение для автоматизированного базового аудита Linux-системы. Программа анализирует систему, выполняет четыре блока проверок и формирует структурированный отчёт:

1. анализ прав доступа к файлам и каталогам;
2. сетевой аудит открытых локальных портов и сервисов;
3. анализ cron-задач;
4. проверка потенциальных CVE на базе открытых сервисов/портов.

Для каждой найденной проблемы приложение формирует краткое описание и рекомендацию по устранению. 

---
## Требования к системе

- Linux (рекомендуется Kali Linux, Debian или Ubuntu);
- Python 3.9 или выше;
- наличие системной утилиты `ss` или `netstat`;
- для полного аудита желательно запускать программу с `sudo`.

### Установка зависимостей

Для GUI-интерфейса (PyQt5):
```bash
sudo apt update
sudo apt install python3-pyqt5
```

Альтернативно через pip (не рекомендуется на системах с apt):
```bash
pip install -r requirements.txt
```

Если PyQt5 недоступен, программа автоматически переключится в консольный режим.
---
## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/IrinaSouthCTF/ITBur-CyberAudit-Sis.Admins.esstu.git
cd ITBur-CyberAudit-Sis.Admins.esstu
```

### 2. Проверка версии Python

```bash
python --version
```

### 3. Запуск программы

#### Консольный режим (всегда доступен):
```bash
python auditor.py --nogui -o audit_result.txt
```

#### Включение CVE-проверки по открытым сервисам:
```bash
python auditor.py --nogui --cve -o audit_result.txt
```

#### Принудительное обновление (с загрузкой из сети):
```bash
python auditor.py --nogui --cve --cve-update -o audit_result.txt
```

#### Использовать локальную базу без обращения к сети:
```bash
python auditor.py --nogui --cve --no-cve-online -o audit_result.txt
```

#### Графический интерфейс (если установлен PyQt5 и есть дисплей):
```bash
python auditor.py
```
Если PyQt5 не установлен или нет GUI, автоматически запустится консольный режим.

Базовый запуск (автоматический выбор режима):

```bash
python auditor.py
```

Запуск с собственным именем отчёта:

```bash
python auditor.py -o audit_result
```

Рекомендуемый запуск для полного охвата системы:

```bash
sudo python auditor.py -o audit_result
```

Почему может понадобиться `sudo`:

- часть каталогов `/etc`, `/var`, `/home` может быть недоступна обычному пользователю;
- без повышенных прав часть сведений о процессах и сокетах может быть скрыта;
- не все cron-файлы доступны для чтения без прав суперпользователя.

---
## Структура проекта

- `auditor.py` - главный файл, выбирает режим запуска (GUI или CLI)
- `auditor_core.py` - ядро с функциями аудита
- `auditor_cli.py` - консольный интерфейс
- `auditor_ui.py` - графический интерфейс на PyQt5
- `README.md` - документация
- `todo.md` - задачи проекта
## Пример работы

```
--------------------------------------------------------------------------------
[5208] Уровень риска: MEDIUM
Проблема: Подозрительный файл доступен на чтение всем
Объект: /home/yota/.mozilla/firefox/b4hsz0qf.default-esr/storage/default/https+++web.telegram.org/idb/2647014652tnwoembm-oc.sqlite-wal
Права: 0o644

Описание:
Имя файла похоже на конфигурационный, ключевой или резервный.

Рекомендация:
Проверьте содержимое файла и при необходимости ограничьте права, например до 640 или 600.
--------------------------------------------------------------------------------

```

```
================================================================================
СВОДКА
================================================================================
Проблем высокого уровня риска: 4538
Проблем среднего уровня риска: 703
Проблем низкого уровня риска: 0
Найдено сетевых замечаний: 4
Найдено проблем с правами: 5237
Найдено замечаний по cron: 0

```

## Автоматическое обновление CVE-базы

Программа поддерживает хранение базы в `cve_db.json` рядом с `auditor_core.py`.
Файл синхронизируется с внешним URL при первом запуске и при устаревании (по умолчанию 7 дней).

Команды:
- `--cve` - включить проверку CVE
- `--cve-update` - принудительно обновить базу
- `--cve-cache-days N` - кэшированная актуальность N дней
- `--no-cve-online` - использовать локальный/встроенный режим

URL по умолчанию: NVD API (через CPE map) при запросе.

Если в `cve_db.json` υπάρχει локальный кэш, программа будет использовать его.

Формат `cve_db.json`:

```json
{
  "meta": {
    "source": "remote",
    "updated_at": "2026-03-25T14:00:00+00:00",
    "version": "1.0"
  },
  "data": {
    "22": [{"id": "CVE-2024-XXXX", "service": "OpenSSH", "level": "high", "description": "...", "recommendation": "..."}],
    "3306": [...]
  }
}
```

